language: node_js
node_js:
  - stable

script:
  - npm install
  - npm test
  - npm run build

deploy:
  provider: surge
  project: ./build/
  skip_cleanup: true
  domain: timerapp.nabeelfarid.surge.sh
  on:
    branch: master
